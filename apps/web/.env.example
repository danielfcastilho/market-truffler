# Only needed when running `npm run dev` directly on the host (outside
# Docker Compose). Points the server-side API proxy/rewrites at the FastAPI
# backend. Docker Compose sets this itself (to http://api:8000).
API_INTERNAL_URL=http://localhost:8000
