import { InProgressPage } from "@/components/in-progress-page";

export default function WarhogPage() {
  return (
    <InProgressPage
      icon="🐗"
      name="Warhog"
      tagline="Trading engine."
      message="Still in the den — no orders will be placed."
    />
  );
}
